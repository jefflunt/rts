# Design and implement the full interactive minimap overlay in `internal/client/minimap.go`, including coordinate translation math (world to/from minimap), a background cache of tile colors, a metallic outer border, a white viewport camera indicator, and left-click/drag input handling to pan and clamp the main camera viewport.

The proposed task is too granular and disjointed because it focuses solely on implementing click-and-drag input interactivity on a component that has not yet been defined or structured. According to the Actionable Heuristic, a task is only actionable if it describes a cohesive Logical Unit of Work (LUoW) that can be implemented and verified as a functional slice. Since `internal/client/minimap.go` does not exist, there is no Minimap struct, coordinate translation math, or rendering/integration to support the input interactivity.

To make this task actionable, we must either expand the scope to implement the interactive minimap component as a whole (covering coordinate translation, background caching, viewport rendering, and click/drag-to-pan input) or break it down such that we implement the foundational model first before adding the interactive layer.

[Need Input]: Since the file `internal/client/minimap.go` and the core Minimap struct/rendering do not exist yet, implementing only the input interactivity is too granular to be a complete, testable Logical Unit of Work (LUoW). Would you like to expand the scope to implement the full Interactive Minimap feature (including the struct, coordinate translation, rendering cache, and camera panning integration) as a single cohesive LUoW, or should we implement the foundational Minimap model and translation logic first?
